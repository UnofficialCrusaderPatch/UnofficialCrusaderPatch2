<Window x:Class="UCP.Views.PathSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UCP.Views"
        mc:Ignorable="d"
        Title="{DynamicResource ui_pathselection}" Height="100" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" x:Name="pTextBoxPath" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10" MaxLines="1" Background="#99FFFFFF" FontSize="14"/>
        <Button Grid.Column="1" x:Name="pButtonSearch" Content="{DynamicResource ui_search}" Click="bPathSearch_Click" Margin="10"/>

    </Grid>
</Window>
